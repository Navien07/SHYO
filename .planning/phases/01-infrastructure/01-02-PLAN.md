---
phase: 01-infrastructure
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - vercel.json
  - .env.local.example
  - src/app/api/revalidate/route.ts
autonomous: false
requirements:
  - INFRA-04
  - INFRA-05
  - ADMIN-04

must_haves:
  truths:
    - "Vercel deployment is live at a preview URL with Singapore (sin1) edge node"
    - "All five environment variables are set in Vercel project settings (not just .env.local)"
    - "Resend account exists with sender domain verification initiated"
    - "Sanity webhook is configured pointing at the Vercel /api/revalidate URL with matching secret"
    - "Publishing a document in Studio triggers ISR revalidation and page updates within seconds"
  artifacts:
    - path: "vercel.json"
      provides: "Singapore region pin"
      contains: "sin1"
    - path: "src/app/api/revalidate/route.ts"
      provides: "ISR revalidation webhook handler"
      contains: "SANITY_REVALIDATE_SECRET"
  key_links:
    - from: "Sanity webhook (manage.sanity.io)"
      to: "https://{vercel-domain}/api/revalidate"
      via: "HTTP POST with HMAC signature"
      pattern: "parseBody"
    - from: "src/app/api/revalidate/route.ts"
      to: "Next.js cache"
      via: "revalidateTag(body._type)"
      pattern: "revalidateTag"

user_setup:
  - service: vercel
    why: "Deploy the Next.js app with Singapore edge node"
    env_vars:
      - name: NEXT_PUBLIC_SANITY_PROJECT_ID
        source: "Sanity project created during Plan 01 execution, visible at manage.sanity.io"
      - name: NEXT_PUBLIC_SANITY_DATASET
        source: "Value: production"
      - name: SANITY_API_TOKEN
        source: "manage.sanity.io -> project -> API -> Tokens -> Add API Token (Viewer role minimum)"
      - name: SANITY_REVALIDATE_SECRET
        source: "Generate with: openssl rand -hex 32"
      - name: RESEND_API_KEY
        source: "resend.com dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Connect GitHub repo to Vercel"
        location: "vercel.com -> Add New Project -> Import Git Repository"
      - task: "Set region to Singapore"
        location: "vercel.json already contains {\"regions\":[\"sin1\"]} — Vercel reads this on deploy"
  - service: resend
    why: "Transactional email delivery for contact form and membership form"
    env_vars:
      - name: RESEND_API_KEY
        source: "resend.com dashboard -> API Keys -> Create API Key (Full access)"
    dashboard_config:
      - task: "Add and verify sender domain"
        location: "resend.com -> Domains -> Add Domain. Enter the org domain (e.g. seputehhyo.org.my). Add the provided MX, TXT (SPF), and DKIM records at your DNS registrar. Verification can take 24-48 hours — start this on Day 1."
  - service: sanity
    why: "Configure ISR webhook to trigger Next.js cache revalidation on publish"
    dashboard_config:
      - task: "Create revalidation webhook"
        location: "manage.sanity.io -> project -> API -> Webhooks -> Add webhook. Set URL to https://{your-vercel-domain}/api/revalidate. Dataset: production. Trigger on: Create, Update, Delete. Filter: empty (all types). Secret: the same SANITY_REVALIDATE_SECRET value set in Vercel."
---

<objective>
Deploy the scaffolded site to Vercel with all environment variables set, configure the Sanity ISR revalidation webhook, and connect Resend for transactional email — verifying that a Sanity publish triggers page revalidation within seconds.

Purpose: The live deployment URL is required for webhook configuration and for end-to-end verification of the ISR flow. This cannot be done locally.

Output: Live Vercel preview URL, working ISR webhook, Resend domain verification initiated.
</objective>

<execution_context>
@C:/Users/navie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/navie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare deployment configuration and generate secrets</name>
  <files>vercel.json, .env.local.example, src/app/api/revalidate/route.ts</files>
  <action>
Step 1 — Verify vercel.json contains the Singapore region pin (should already exist from Plan 01):
```json
{
  "regions": ["sin1"]
}
```

Step 2 — Generate SANITY_REVALIDATE_SECRET. Run in terminal and save the output:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
Save this value — it must be set identically in both Vercel environment variables AND the Sanity webhook secret field.

Step 3 — Verify `src/app/api/revalidate/route.ts` was created in Plan 01 with the correct signature. It should use `parseBody` from `next-sanity/webhook` and call `revalidateTag(body._type)`. No changes needed unless the file is missing.

Step 4 — Update `.env.local.example` to include a comment about the revalidate secret:
```
# Copy to .env.local and fill in values (never commit .env.local)
NEXT_PUBLIC_SANITY_PROJECT_ID=    # From manage.sanity.io project settings
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=                  # Viewer token from manage.sanity.io -> API -> Tokens
SANITY_REVALIDATE_SECRET=          # Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
RESEND_API_KEY=                    # From resend.com dashboard -> API Keys
```

Step 5 — Commit all changes so the Vercel deploy has the latest code:
```bash
git add -A
git commit -m "feat: complete project scaffold with all schemas and routing"
git push origin master
```

Step 6 — Install Vercel CLI and deploy:
```bash
npm install -g vercel
vercel --yes
```
Follow interactive prompts: link to existing project or create new. Choose the org/account linked to the org email.

Step 7 — After deploy completes, set all five environment variables in Vercel:
```bash
vercel env add NEXT_PUBLIC_SANITY_PROJECT_ID production
vercel env add NEXT_PUBLIC_SANITY_DATASET production
vercel env add SANITY_API_TOKEN production
vercel env add SANITY_REVALIDATE_SECRET production
vercel env add RESEND_API_KEY production
```
Enter each value when prompted.

Step 8 — Trigger a production deploy with env vars applied:
```bash
vercel --prod
```
  </action>
  <verify>
    <automated>vercel ls --scope . 2>/dev/null | head -5 || echo "Verify via Vercel dashboard"</automated>
  </verify>
  <done>Vercel deployment URL is live and returns 200. All five env vars are set in Vercel project settings. Generated SANITY_REVALIDATE_SECRET is saved securely (will be used in webhook setup).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployment, ISR webhook, and Resend domain setup</name>
  <action>Human verification of live Vercel deployment, Sanity ISR webhook, and Resend domain registration. See how-to-verify for exact steps.</action>
  <what-built>
    - Site deployed to Vercel with Singapore (sin1) edge node
    - All environment variables set in Vercel project settings
    - Sanity webhook configured at manage.sanity.io pointing to the Vercel /api/revalidate URL
    - Resend account created and sender domain verification initiated (DNS records added)
  </what-built>
  <how-to-verify>
    Step 1: Visit your Vercel deployment URL (shown by `vercel --prod` output). Confirm it loads.

    Step 2: Navigate to `{vercel-url}/en` — should return the placeholder homepage.

    Step 3: Navigate to `{vercel-url}/studio` — should load Sanity Studio (NOT redirect to /en/studio).

    Step 4: In manage.sanity.io, set up the revalidation webhook:
      - Navigate to your project -> API -> Webhooks -> Add webhook
      - URL: `https://{your-vercel-domain}/api/revalidate`
      - Dataset: production
      - Trigger on: Create, Update, Delete
      - Filter: (leave empty)
      - Secret: (paste the SANITY_REVALIDATE_SECRET value you generated)
      - Save

    Step 5: Test ISR revalidation:
      - In Studio at /studio, open Site Settings and add a test value to any field, then Publish
      - Check Vercel function logs for the /api/revalidate call (Vercel dashboard -> Functions -> /api/revalidate)
      - Confirm the log shows `{ revalidated: true, type: "siteSettings" }` (not 401 Invalid signature)

    Step 6: Confirm Resend domain verification:
      - In resend.com dashboard -> Domains, confirm the org domain is added
      - DNS records (MX, TXT, DKIM) are added at the registrar
      - Status may show "Pending" — this is expected (DNS propagation takes up to 48h)
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification required — see how-to-verify steps above"</automated>
  </verify>
  <done>Deployment live, ISR webhook confirmed working (no 401 in Vercel logs), Resend domain added with DNS records submitted.</done>
  <resume-signal>Type "approved" once deployment is live, webhook is configured, and Resend domain is pending verification. Describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
- Vercel deployment URL returns 200 for /en/, /ms/, /ta/ paths
- /studio loads without locale redirect
- Sanity webhook POST to /api/revalidate returns `{"revalidated":true}` (not 401)
- Resend domain added (pending DNS verification is acceptable)
</verification>

<success_criteria>
1. Live Vercel deployment URL accessible with all three locale routes returning 200
2. ISR webhook confirmed working: Sanity publish triggers revalidateTag (visible in Vercel function logs)
3. Resend account created and domain verification initiated (DNS records submitted)
4. All five environment variables set in Vercel project settings
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md` documenting:
- Vercel deployment URL
- Environment variables set (names only, not values)
- Webhook test result (request log confirmation)
- Resend domain verification status
</output>
