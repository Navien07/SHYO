---
phase: 02-shared-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/globals.css
  - src/app/[locale]/layout.tsx
  - src/i18n/navigation.ts
  - src/components/layout/Header.tsx
  - src/components/layout/Nav.tsx
  - src/components/layout/MobileDrawer.tsx
  - src/components/layout/LanguageSwitcher.tsx
  - src/lib/sanity/queries.ts
  - public/logo.svg
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-04
  - BRAND-05
  - BRAND-08
  - BRAND-09
  - I18N-01
  - I18N-04
  - I18N-05

must_haves:
  truths:
    - "Tailwind classes `bg-brand-green`, `text-brand-green-dark`, `bg-brand-charcoal` resolve at compile time (no unknown token warnings)"
    - "Tamil pages load Noto Sans Tamil font; non-Tamil pages load Inter — verified by DevTools Fonts panel"
    - "Sticky nav is present on every page, stays fixed at top on scroll, has solid white background at all times"
    - "Language switcher renders 3 always-visible pills: English | Bahasa Malaysia | தமிழ்; active locale is green-highlighted"
    - "Hamburger button on mobile opens a right-side sheet drawer containing all 7 nav links and the language switcher"
    - "GROQ query helpers compile without TypeScript errors and all 4 content-type fetch functions are exported"
    - "Logo placeholder SVG exists in public/ and is rendered in the nav (text fallback if no logo supplied)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand token @theme block + @layer base Tamil font rule"
      contains: "--color-brand-green: #32a852"
    - path: "src/i18n/navigation.ts"
      provides: "createNavigation exports for locale-aware Link, useRouter, usePathname"
      exports: ["Link", "redirect", "usePathname", "useRouter", "getPathname"]
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky header wrapper containing Nav and LanguageSwitcher"
      contains: "sticky top-0 z-50"
    - path: "src/components/layout/Nav.tsx"
      provides: "Desktop nav links + hamburger trigger for mobile"
    - path: "src/components/layout/MobileDrawer.tsx"
      provides: "shadcn Sheet — right-side slide-in drawer with 7 nav links + LanguageSwitcher"
    - path: "src/components/layout/LanguageSwitcher.tsx"
      provides: "Inline pill switcher; uses router.replace for locale switch (I18N-05)"
      contains: "தமிழ்"
    - path: "src/lib/sanity/queries.ts"
      provides: "Typed GROQ fetch helpers: getSiteSettings, getAllProgrammes, getAllTeamMembers, getAllDocuments"
      exports: ["getSiteSettings", "getAllProgrammes", "getAllTeamMembers", "getAllDocuments"]
    - path: "public/logo.svg"
      provides: "Placeholder SVG logo (text: Seputeh HYO) — replaced by real logo asset in Phase 5"
  key_links:
    - from: "src/i18n/navigation.ts"
      to: "src/i18n/routing.ts"
      via: "createNavigation(routing)"
      pattern: "createNavigation\\(routing\\)"
    - from: "src/components/layout/LanguageSwitcher.tsx"
      to: "src/i18n/navigation.ts"
      via: "import { useRouter, usePathname } from '@/i18n/navigation'"
      pattern: "@/i18n/navigation"
    - from: "src/app/[locale]/layout.tsx"
      to: "next/font/google"
      via: "Noto_Sans_Tamil({ variable: '--font-noto-sans-tamil' })"
      pattern: "notoSansTamil\\.variable"
    - from: "src/lib/sanity/queries.ts"
      to: "src/sanity/client.ts"
      via: "import { sanityFetch } from '@/sanity/client'"
      pattern: "sanityFetch"
---

<objective>
Build the brand token layer, navigation shell, and GROQ query helpers. This creates the visual and data foundation that the Footer and locale layout (Plan 03) will consume.

Purpose: No downstream page can be built until brand tokens resolve, the nav exists, and Sanity fetch helpers are typed.
Output: globals.css extended with brand @theme block, navigation.ts created, Header/Nav/MobileDrawer/LanguageSwitcher components, GROQ helpers file, placeholder logo SVG, and locale layout updated with font variables.
</objective>

<execution_context>
@C:/Users/navie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/navie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-ui/02-CONTEXT.md
@.planning/phases/02-shared-ui/02-RESEARCH.md

<interfaces>
<!-- Existing globals.css — append brand @theme block AFTER @theme inline block (line 48); do NOT modify the existing @theme inline block or :root variables -->
Current globals.css structure:
- Lines 1-5: @import statements + @custom-variant dark
- Lines 7-48: @theme inline { ... } — shadcn block, DO NOT TOUCH
- Lines 50-83: :root { ... } — shadcn variables, DO NOT TOUCH
- Lines 85-117: .dark { ... } — shadcn dark vars, DO NOT TOUCH
- Lines 119-129: @layer base { * { @apply border-border... } body { @apply bg-background... } html { @apply font-sans; } }

Append after line 129:
1. A new standalone `@theme { ... }` block for brand tokens
2. Extend existing `@layer base` with html { font-size: 16px } and html[lang="ta"] { ... }

<!-- Existing locale layout — must be extended, not replaced -->
From src/app/[locale]/layout.tsx (current):
```tsx
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { notFound } from 'next/navigation';
import { routing } from '../../i18n/routing';

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({ children, params }) {
  const { locale } = await params;
  if (!routing.locales.includes(locale as 'en' | 'ms' | 'ta')) { notFound(); }
  const messages = await getMessages();
  return (
    <html lang={locale}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```
Add: Inter + Noto_Sans_Tamil font imports, `className={${inter.variable} ${notoSansTamil.variable}}` on `<html>`, `overflow-x-hidden` on `<body>`.
DO NOT add Header/Footer here yet — that is Plan 03 Task 1.

<!-- Existing routing — navigation.ts wraps this -->
From src/i18n/routing.ts:
```typescript
export const routing = defineRouting({ locales: ['en', 'ms', 'ta'], defaultLocale: 'en' });
```

<!-- Existing Sanity client — queries.ts imports this -->
From src/sanity/client.ts:
```typescript
export async function sanityFetch<T>({ query, params = {}, tags }: {...}): Promise<T> {
  return client.fetch<T>(query, params, { next: { tags }, cache: 'force-cache' });
}
```

<!-- Installed but not yet configured: shadcn Sheet -->
Sheet is NOT yet installed. Install it FIRST before creating MobileDrawer:
```bash
npx shadcn@latest add sheet
```

<!-- Available installed libraries -->
- lucide-react (^0.577.0): Menu, X icons for hamburger
- clsx + tailwind-merge via cn() in src/lib/utils.ts
- @sanity/image-url (^1.2.0): for image URL building in queries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand tokens in globals.css + locale layout font wiring</name>
  <files>src/app/globals.css, src/app/[locale]/layout.tsx</files>
  <action>
    STEP A — globals.css: Append the following block at the end of the file (after the existing @layer base block). Do NOT modify anything above line 129.

    ```css
    /* === Seputeh HYO Brand Tokens === */
    /* Append after existing @layer base block */

    @theme {
      /* Brand colours — generates bg-brand-green, text-brand-green, border-brand-green, etc. */
      --color-brand-green: #32a852;
      --color-brand-green-dark: #1e7a36;   /* Hover states; white text = ~5.1:1 WCAG AA ✓ */
      --color-brand-saffron: #c96a00;      /* CTAs; black text on saffron bg ≈ 5.2:1 WCAG AA ✓ */
      --color-brand-charcoal: #1a1a1a;    /* Footer background */
      --color-brand-charcoal-text: #f5f5f5; /* Footer text */
    }

    /* Tamil locale font and line-height override */
    @layer base {
      html {
        font-size: 16px;
        line-height: 1.5;
      }

      html[lang="ta"] {
        font-family: var(--font-noto-sans-tamil), sans-serif;
        line-height: 1.7;
      }
    }
    ```

    CRITICAL: `#32a852` MUST NOT be used as text colour on white. It is for:
    - Decorative `border-b-2 border-brand-green` on active nav links (decorative, no contrast req)
    - `bg-brand-green-dark` (#1e7a36) for button backgrounds with white text
    - `bg-brand-green` backgrounds with white text on very dark green only

    STEP B — locale layout: Replace src/app/[locale]/layout.tsx with the following. Preserve all existing logic (generateStaticParams, locale validation, messages, NextIntlClientProvider):

    ```tsx
    import { NextIntlClientProvider } from 'next-intl';
    import { getMessages } from 'next-intl/server';
    import { notFound } from 'next/navigation';
    import { Inter, Noto_Sans_Tamil } from 'next/font/google';
    import { routing } from '../../i18n/routing';

    const inter = Inter({
      subsets: ['latin'],
      display: 'swap',
      variable: '--font-noto-sans',
    });

    const notoSansTamil = Noto_Sans_Tamil({
      subsets: ['tamil'],
      display: 'swap',
      variable: '--font-noto-sans-tamil',
    });

    export function generateStaticParams() {
      return routing.locales.map((locale) => ({ locale }));
    }

    export default async function LocaleLayout({
      children,
      params,
    }: {
      children: React.ReactNode;
      params: Promise<{ locale: string }>;
    }) {
      const { locale } = await params;
      if (!routing.locales.includes(locale as 'en' | 'ms' | 'ta')) {
        notFound();
      }
      const messages = await getMessages();
      return (
        <html
          lang={locale}
          className={`${inter.variable} ${notoSansTamil.variable}`}
        >
          <body className="overflow-x-hidden">
            <NextIntlClientProvider messages={messages}>
              {children}
            </NextIntlClientProvider>
          </body>
        </html>
      );
    }
    ```

    Note: Header and Footer are added in Plan 03 Task 1 — NOT here. This task only wires fonts and overflow.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/brand-tokens.test.ts tests/ui/tamil-font.test.ts tests/ui/responsive.test.ts</automated>
  </verify>
  <done>brand-tokens, tamil-font, and responsive tests pass GREEN. `npx next build` (or `npx next dev` startup) does not error on the globals.css changes.</done>
</task>

<task type="auto">
  <name>Task 2: navigation.ts + Header/Nav/LanguageSwitcher/MobileDrawer components + placeholder logo</name>
  <files>
    src/i18n/navigation.ts,
    src/components/layout/Header.tsx,
    src/components/layout/Nav.tsx,
    src/components/layout/LanguageSwitcher.tsx,
    src/components/layout/MobileDrawer.tsx,
    public/logo.svg
  </files>
  <action>
    STEP A — Install shadcn Sheet (required for MobileDrawer):
    ```bash
    npx shadcn@latest add sheet
    ```
    This generates src/components/ui/sheet.tsx. Confirm it exists before proceeding.

    STEP B — src/i18n/navigation.ts (new file):
    ```typescript
    import { createNavigation } from 'next-intl/navigation';
    import { routing } from './routing';

    export const { Link, redirect, usePathname, useRouter, getPathname } =
      createNavigation(routing);
    ```

    STEP C — public/logo.svg (placeholder — no real logo asset exists yet):
    Create a minimal SVG that spells out "Seputeh HYO" in brand green. This is a development placeholder; the real logo replaces it in Phase 5.
    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 160 40" width="160" height="40">
      <text x="4" y="28" font-family="sans-serif" font-size="18" font-weight="700" fill="#1e7a36">Seputeh HYO</text>
    </svg>
    ```

    STEP D — src/components/layout/LanguageSwitcher.tsx (new file, client component):
    CRITICAL: import usePathname and useRouter from '@/i18n/navigation' NOT from 'next/navigation'.
    Using next/navigation version causes locale-stacking bug (/en/ms/about).

    ```tsx
    'use client';

    import { useRouter, usePathname } from '@/i18n/navigation';
    import { useParams } from 'next/navigation';
    import { useLocale } from 'next-intl';
    import { cn } from '@/lib/utils';

    const LOCALES = [
      { code: 'en', label: 'English' },
      { code: 'ms', label: 'Bahasa Malaysia' },
      { code: 'ta', label: 'தமிழ்' },
    ] as const;

    export function LanguageSwitcher() {
      const router = useRouter();
      const pathname = usePathname();
      const params = useParams();
      const currentLocale = useLocale();

      const handleLocaleChange = (locale: string) => {
        router.replace(
          // @ts-expect-error — params type from next/navigation is broader than next-intl expects
          { pathname, params },
          { locale }
        );
      };

      return (
        <div className="flex gap-1 flex-wrap" role="group" aria-label="Language switcher">
          {LOCALES.map(({ code, label }) => (
            <button
              key={code}
              onClick={() => handleLocaleChange(code)}
              className={cn(
                'px-3 py-1 rounded-full text-sm font-medium min-h-[44px] min-w-[44px] transition-colors',
                currentLocale === code
                  ? 'bg-brand-green-dark text-white'
                  : 'text-gray-600 hover:text-brand-green-dark border border-gray-200 hover:border-brand-green'
              )}
              aria-current={currentLocale === code ? 'true' : undefined}
              aria-pressed={currentLocale === code}
            >
              {label}
            </button>
          ))}
        </div>
      );
    }
    ```

    STEP E — src/components/layout/MobileDrawer.tsx (new file, client component):
    ```tsx
    'use client';

    import { useState } from 'react';
    import { Menu } from 'lucide-react';
    import {
      Sheet,
      SheetContent,
      SheetHeader,
      SheetTitle,
    } from '@/components/ui/sheet';
    import { LanguageSwitcher } from './LanguageSwitcher';

    interface MobileDrawerProps {
      navLinks: React.ReactNode;
    }

    export function MobileDrawer({ navLinks }: MobileDrawerProps) {
      const [open, setOpen] = useState(false);

      return (
        <Sheet open={open} onOpenChange={setOpen}>
          <button
            onClick={() => setOpen(true)}
            aria-label="Open navigation menu"
            aria-expanded={open}
            className="min-h-[44px] min-w-[44px] flex items-center justify-center rounded-md hover:bg-gray-100 transition-colors lg:hidden"
          >
            <Menu className="h-6 w-6 text-gray-700" aria-hidden="true" />
          </button>
          <SheetContent side="right" className="w-[300px] flex flex-col">
            <SheetHeader>
              <SheetTitle className="sr-only">Navigation Menu</SheetTitle>
            </SheetHeader>
            <nav className="flex flex-col gap-2 mt-6 flex-1" aria-label="Mobile navigation">
              {navLinks}
            </nav>
            <div className="pt-6 border-t border-gray-200 pb-4">
              <p className="text-xs text-gray-500 mb-3 uppercase tracking-wide">Language</p>
              <LanguageSwitcher />
            </div>
          </SheetContent>
        </Sheet>
      );
    }
    ```

    STEP F — src/components/layout/Nav.tsx (new file — contains both desktop and mobile nav):
    Nav is a Server Component. It passes nav link nodes to MobileDrawer.
    Nav links use Link from '@/i18n/navigation' for locale-aware routing.

    ```tsx
    import Link from 'next/link';
    import Image from 'next/image';
    import { useTranslations } from 'next-intl';
    import { LanguageSwitcher } from './LanguageSwitcher';
    import { MobileDrawer } from './MobileDrawer';

    const NAV_LINKS = [
      { key: 'home', href: '/' },
      { key: 'about', href: '/about' },
      { key: 'programmes', href: '/programmes' },
      { key: 'team', href: '/team' },
      { key: 'documents', href: '/documents' },
      { key: 'contact', href: '/contact' },
      { key: 'membership', href: '/membership' },
    ] as const;

    export function Nav() {
      const t = useTranslations('nav');

      const navLinkElements = NAV_LINKS.map(({ key, href }) => (
        <Link
          key={key}
          href={href}
          className="block px-3 py-2 text-gray-800 font-medium text-base hover:text-brand-green-dark transition-colors rounded-md hover:bg-gray-50 min-h-[44px] flex items-center lg:inline-flex lg:px-2 lg:py-1 lg:rounded-none lg:border-b-2 lg:border-transparent lg:hover:border-brand-green lg:hover:bg-transparent"
        >
          {t(key)}
        </Link>
      ));

      return (
        <div className="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16">
          {/* Logo left */}
          <Link href="/" className="flex items-center gap-2 shrink-0 min-h-[44px]" aria-label="Seputeh HYO — Home">
            <Image
              src="/logo.svg"
              alt="Seputeh HYO logo"
              width={140}
              height={36}
              priority
            />
          </Link>

          {/* Desktop nav links — hidden on mobile */}
          <nav className="hidden lg:flex items-center gap-1" aria-label="Main navigation">
            {navLinkElements}
          </nav>

          {/* Desktop language switcher — hidden on mobile */}
          <div className="hidden lg:flex items-center">
            <LanguageSwitcher />
          </div>

          {/* Mobile hamburger — hidden on desktop */}
          <MobileDrawer navLinks={navLinkElements} />
        </div>
      );
    }
    ```

    STEP G — src/components/layout/Header.tsx (new file):
    Header is a thin sticky wrapper. It must have `sticky top-0 z-50` for BRAND-05.
    ```tsx
    import { Nav } from './Nav';

    export function Header() {
      return (
        <header className="sticky top-0 z-50 w-full bg-white border-b border-gray-200">
          <Nav />
        </header>
      );
    }
    ```

    IMPORTANT — Nav.tsx uses `useTranslations` which is a server-side next-intl hook — Nav must NOT have 'use client'. MobileDrawer and LanguageSwitcher are 'use client' because they use useState/useRouter/useLocale. This split is correct and expected.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/layout.test.ts tests/ui/lang-switcher.test.ts</automated>
  </verify>
  <done>layout.test.ts and lang-switcher.test.ts both pass GREEN. Header.tsx exists with `sticky top-0`, navigation.ts exports createNavigation result, LanguageSwitcher.tsx contains all 3 locale labels.</done>
</task>

<task type="auto">
  <name>Task 3: GROQ typed query helpers</name>
  <files>src/lib/sanity/queries.ts</files>
  <action>
    Create src/lib/sanity/ directory if it does not exist.
    Create src/lib/sanity/queries.ts:

    ```typescript
    import { defineQuery } from 'next-sanity';
    import { sanityFetch } from '@/sanity/client';

    // --- Shared Types ---
    export type LocalizedString = {
      en?: string;
      ms?: string;
      ta?: string;
    };

    export type SiteSettings = {
      _id: string;
      contactEmail?: string;
      whatsappNumber?: string;
      facebookUrl?: string;
      instagramUrl?: string;
    };

    export type Programme = {
      _id: string;
      title: LocalizedString;
      description: LocalizedString;
      image?: { asset: { _ref: string } };
      category: string;
      slug: { current: string };
    };

    export type TeamMember = {
      _id: string;
      name: LocalizedString;
      role: LocalizedString;
      photo?: { asset: { _ref: string } };
      order?: number;
    };

    export type SanityDocument = {
      _id: string;
      title: LocalizedString;
      category: string;
      year: number;
      file?: { asset: { _ref: string } };
      uploadDate?: string;
    };

    // --- GROQ Queries (defineQuery enables Sanity TypeGen) ---
    export const SITE_SETTINGS_QUERY = defineQuery(
      `*[_type == "siteSettings"][0]{ _id, contactEmail, whatsappNumber, facebookUrl, instagramUrl }`
    );

    export const ALL_PROGRAMMES_QUERY = defineQuery(
      `*[_type == "programme"] | order(_createdAt desc) { _id, title, description, image, category, slug }`
    );

    export const ALL_TEAM_MEMBERS_QUERY = defineQuery(
      `*[_type == "teamMember"] | order(order asc, _createdAt asc) { _id, name, role, photo, order }`
    );

    export const ALL_DOCUMENTS_QUERY = defineQuery(
      `*[_type == "document"] | order(year desc, uploadDate desc) { _id, title, category, year, file, uploadDate }`
    );

    // --- Typed Fetch Helpers ---

    /** Fetches siteSettings singleton. Cached with ISR tag 'siteSettings'. */
    export async function getSiteSettings(): Promise<SiteSettings | null> {
      return sanityFetch<SiteSettings | null>({
        query: SITE_SETTINGS_QUERY,
        tags: ['siteSettings'],
      });
    }

    /** Fetches all programmes ordered by creation date desc. Cached with ISR tag 'programme'. */
    export async function getAllProgrammes(): Promise<Programme[]> {
      return sanityFetch<Programme[]>({
        query: ALL_PROGRAMMES_QUERY,
        tags: ['programme'],
      });
    }

    /** Fetches all team members ordered by manual order asc. Cached with ISR tag 'teamMember'. */
    export async function getAllTeamMembers(): Promise<TeamMember[]> {
      return sanityFetch<TeamMember[]>({
        query: ALL_TEAM_MEMBERS_QUERY,
        tags: ['teamMember'],
      });
    }

    /** Fetches all documents ordered by year desc then uploadDate desc. Cached with ISR tag 'document'. */
    export async function getAllDocuments(): Promise<SanityDocument[]> {
      return sanityFetch<SanityDocument[]>({
        query: ALL_DOCUMENTS_QUERY,
        tags: ['document'],
      });
    }
    ```

    The `sanityFetch` helper in src/sanity/client.ts uses `cache: 'force-cache'` with `next: { tags }` — this integrates with the existing ISR webhook revalidation from Phase 1.
    Do NOT use `next-sanity`'s built-in `sanityFetch` wrapper — use the project-local one from `src/sanity/client.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/ && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>src/lib/sanity/queries.ts compiles without TypeScript errors. All tests/ui/ tests that were GREEN remain GREEN. TypeScript reports no errors in the queries.ts file.</done>
</task>

</tasks>

<verification>
Full Wave 2 test pass:
```
npx vitest run tests/ui/brand-tokens.test.ts tests/ui/tamil-font.test.ts tests/ui/responsive.test.ts tests/ui/layout.test.ts tests/ui/lang-switcher.test.ts
```
TypeScript compile check:
```
npx tsc --noEmit
```
</verification>

<success_criteria>
- brand-tokens, tamil-font, responsive, layout, and lang-switcher tests all GREEN
- `npx tsc --noEmit` reports no errors
- Header.tsx has `sticky top-0 z-50` class — nav is sticky
- LanguageSwitcher.tsx uses `@/i18n/navigation` (NOT next/navigation) for usePathname/useRouter
- Nav.tsx server component renders 7 nav links with translation keys
- MobileDrawer renders shadcn Sheet with side="right" and SheetTitle with sr-only
- src/lib/sanity/queries.ts exports all 4 fetch helpers with typed return values
- public/logo.svg exists (placeholder SVG)
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-ui/02-02-SUMMARY.md`
</output>
