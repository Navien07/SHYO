---
phase: 03-core-static-pages
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/pages/homepage.test.ts
  - tests/pages/about.test.ts
  - tests/pages/contact.test.ts
  - tests/sanity/schemas.test.ts
  - tests/ui/i18n-messages.test.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06

must_haves:
  truths:
    - "All Phase 3 requirements have automated test coverage from the start (Nyquist compliance)"
    - "Tests fail RED before implementation begins — guards against false positives"
    - "siteSettings schema tests cover the 3 new fields required by Phase 3"
    - "i18n message tests cover the home, about, and contact namespaces in all 3 locales"
  artifacts:
    - path: "tests/pages/homepage.test.ts"
      provides: "Structural + unit tests for HOME-01 through HOME-04"
    - path: "tests/pages/about.test.ts"
      provides: "Structural tests for ABOUT-01 through ABOUT-04"
    - path: "tests/pages/contact.test.ts"
      provides: "Structural + unit tests for CONT-01 through CONT-06"
    - path: "tests/sanity/schemas.test.ts"
      provides: "Extended siteSettings assertions for heroImage, memberCount, programmesDelivered"
    - path: "tests/ui/i18n-messages.test.ts"
      provides: "Extended namespace assertions for home, about, contact in EN/BM/Tamil"
  key_links:
    - from: "tests/pages/contact.test.ts"
      to: "src/app/[locale]/contact/actions.ts"
      via: "fs.existsSync structural check"
      pattern: "existsSync.*contact/actions"
    - from: "tests/sanity/schemas.test.ts"
      to: "src/sanity/schemas/siteSettings.ts"
      via: "direct import + field assertion"
      pattern: "heroImage|memberCount|programmesDelivered"
---

<objective>
Write all Phase 3 test stubs in RED state before any implementation begins.

Purpose: Nyquist compliance — every requirement has automated verification before code is written. RED state is mandatory; tests must fail now and pass only after implementation.
Output: 5 test files (3 new, 2 extended) covering all 15 Phase 3 requirements via structural file checks and unit logic tests.
</objective>

<execution_context>
@C:/Users/navie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/navie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-static-pages/03-CONTEXT.md
@.planning/phases/03-core-static-pages/03-RESEARCH.md
@.planning/phases/03-core-static-pages/03-VALIDATION.md
</context>

<interfaces>
<!-- Existing test patterns to follow — all tests use node environment, fs.readFileSync/existsSync, no DOM/React imports -->

From tests/sanity/schemas.test.ts (existing pattern to extend):
```typescript
import { describe, it, expect } from 'vitest';
import { siteSettings } from '../../src/sanity/schemas/siteSettings';

function getFieldNames(schema: { fields?: Array<{ name: string }> }): string[] {
  return (schema.fields ?? []).map(f => f.name);
}
// New assertions to add:
it('has heroImage field', () => expect(getFieldNames(siteSettings)).toContain('heroImage'));
it('has memberCount field', () => expect(getFieldNames(siteSettings)).toContain('memberCount'));
it('has programmesDelivered field', () => expect(getFieldNames(siteSettings)).toContain('programmesDelivered'));
```

From tests/ui/i18n-messages.test.ts (existing pattern to extend):
```typescript
// loadMessages() helper already defined — reuse it
// Add REQUIRED_HOME_KEYS, REQUIRED_ABOUT_KEYS, REQUIRED_CONTACT_KEYS arrays
// Loop over ['en', 'ms', 'ta'] as existing tests do
```

Test environment: Vitest node environment, include: tests/**/*.test.ts
Run command: npx vitest run
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write homepage, about, and contact test stubs (RED)</name>
  <files>tests/pages/homepage.test.ts, tests/pages/about.test.ts, tests/pages/contact.test.ts</files>
  <behavior>
    - homepage.test.ts:
      - `src/app/[locale]/page.tsx` file contains "HeroSection" string (structural — confirms hero is wired)
      - `src/app/[locale]/page.tsx` file contains "ImpactStats" string (structural)
      - `src/app/[locale]/page.tsx` file contains "ProgrammeHighlights" string (structural)
      - `src/app/[locale]/page.tsx` file contains "membership" string (structural — CTA links to /membership)
      - `src/components/home/HeroSection.tsx` file exists (structural)
      - `src/components/home/ImpactStats.tsx` file exists (structural)
      - `src/components/home/ProgrammeHighlights.tsx` file exists (structural)
      - years-active unit test: `new Date().getFullYear() - 2002` is >= 24 and is a number (HOME-02 logic)
    - about.test.ts:
      - `src/app/[locale]/about/page.tsx` file exists (structural)
      - `messages/en.json` about namespace has keys: `title`, `foundingDate`, `missionHeading`, `visionHeading`, `mission`, `vision`, `focusAreasHeading`, `registrationHeading`, `registrationNumber`
    - contact.test.ts:
      - `src/app/[locale]/contact/page.tsx` file exists (structural)
      - `src/app/[locale]/contact/ContactForm.tsx` file exists (structural)
      - `src/app/[locale]/contact/actions.ts` file exists (structural)
      - `src/app/[locale]/contact/actions.ts` contains "sendContactForm" (export name check)
      - `src/app/[locale]/contact/actions.ts` contains "use server" (directive check)
      - `src/app/[locale]/contact/page.tsx` contains "wa.me" string (WhatsApp link — CONT-03)
      - `src/app/[locale]/contact/page.tsx` contains "iframe" or "google.com/maps" (map embed — CONT-06)
      - Zod schema unit test: import Zod schema from actions.ts — SKIP (node import of 'use server' files is not safe in this test env); instead check file contains 'z.object' and 'z.string().email()' via fs.readFileSync
  </behavior>
  <action>
    Create three test files using fs.readFileSync/existsSync (no DOM, no React imports — consistent with Phase 2 test patterns).

    For homepage.test.ts: Use `fs.existsSync` to check component file existence; use `fs.readFileSync` to verify string presence in page.tsx. The years-active logic test directly evaluates `new Date().getFullYear() - 2002` — no import needed.

    For about.test.ts: Use `fs.existsSync` for page file; load `messages/en.json` via `fs.readFileSync` and verify about namespace keys are present.

    For contact.test.ts: Use `fs.existsSync` for page, ContactForm, and actions.ts files; use `fs.readFileSync` on actions.ts to check for 'use server', 'sendContactForm', 'z.object', 'z.string().email()'; use `fs.readFileSync` on page.tsx to check for 'wa.me' and map embed.

    All paths resolved relative to `process.cwd()` (repo root), e.g. `path.resolve(process.cwd(), 'src/app/[locale]/contact/page.tsx')`.

    These tests MUST fail (RED) when created — the implementation files do not exist yet. Run `npx vitest run tests/pages/` to confirm all tests fail with "file does not exist" or similar before committing.
  </action>
  <verify>
    <automated>npx vitest run tests/pages/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in tests/pages/ exist and fail RED (not pass, not error-on-syntax). `npx vitest run tests/pages/` exits with non-zero code and shows test failures, not syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend existing test files for siteSettings new fields and i18n Phase 3 namespaces (RED)</name>
  <files>tests/sanity/schemas.test.ts, tests/ui/i18n-messages.test.ts</files>
  <action>
    Extend `tests/sanity/schemas.test.ts` — add three new `it()` assertions inside the existing `describe('siteSettings schema', ...)` block:
    ```
    it('has heroImage field', () => expect(getFieldNames(siteSettings)).toContain('heroImage'));
    it('has memberCount field', () => expect(getFieldNames(siteSettings)).toContain('memberCount'));
    it('has programmesDelivered field', () => expect(getFieldNames(siteSettings)).toContain('programmesDelivered'));
    ```
    Do NOT change the schema registry length assertion (still 4 schemas — no new schema documents in Phase 3).

    Extend `tests/ui/i18n-messages.test.ts` — add three new required key arrays and corresponding `for` loops AFTER the existing ones:
    ```typescript
    const REQUIRED_HOME_KEYS = ['heroHeadline', 'heroSubline', 'joinUs', 'learnMore', 'impactYears', 'impactMembers', 'impactProgrammes', 'programmesHeading', 'viewAll', 'comingSoon'];
    const REQUIRED_ABOUT_KEYS = ['title', 'foundingDate', 'missionHeading', 'visionHeading', 'mission', 'vision', 'focusAreasHeading', 'registrationHeading', 'registrationNumber'];
    const REQUIRED_CONTACT_KEYS = ['title', 'formName', 'formEmail', 'formSubject', 'formMessage', 'formSubmit', 'successMessage', 'whatsappLabel', 'emailLabel', 'mapLabel', 'followUs'];
    ```
    Loop structure: same pattern as existing nav/footer loops — `for (const key of REQUIRED_HOME_KEYS) { it('has home.{key}', ...) }` inside the locale loop, testing `messages?.home?.[key]`.

    These tests MUST fail RED until Plan 02 adds the message keys. Run `npx vitest run tests/ui/i18n-messages.test.ts` to confirm new assertions fail.
  </action>
  <verify>
    <automated>npx vitest run tests/sanity/schemas.test.ts tests/ui/i18n-messages.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>New siteSettings field assertions fail RED (field not in schema yet). New i18n namespace assertions fail RED (keys not in message files yet). Previously passing tests remain green (no regression). Full suite: `npx vitest run` shows new failures only on the newly added assertions.</done>
</task>

</tasks>

<verification>
Run full test suite: `npx vitest run --reporter=verbose`

Expected state after this plan:
- All new tests (homepage, about, contact stubs + extended schema + extended i18n) FAIL RED
- All Phase 2 tests remain GREEN (no regression)
- Zero syntax errors in any test file
</verification>

<success_criteria>
- `tests/pages/homepage.test.ts`, `tests/pages/about.test.ts`, `tests/pages/contact.test.ts` exist with failing tests
- `tests/sanity/schemas.test.ts` has 3 new failing assertions for heroImage/memberCount/programmesDelivered
- `tests/ui/i18n-messages.test.ts` has failing assertions for home/about/contact namespaces in all 3 locales
- `npx vitest run` exits non-zero (new tests fail) but with zero syntax errors
- All pre-existing Phase 2 tests remain passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-static-pages/03-01-SUMMARY.md`
</output>
