---
phase: 03-core-static-pages
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/[locale]/about/page.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04

must_haves:
  truths:
    - "Visiting /en/about, /ms/about, and /ta/about each renders the About Us page in the correct language"
    - "The founding date 16 September 2002 is visible on the page"
    - "Mission and vision statements are present"
    - "All five focus areas are listed"
    - "The organisation registration number PPM-003-10-20092002 is visible"
  artifacts:
    - path: "src/app/[locale]/about/page.tsx"
      provides: "About Us Server Component with all four ABOUT-01..04 requirements"
      min_lines: 60
  key_links:
    - from: "src/app/[locale]/about/page.tsx"
      to: "next-intl getTranslations('about')"
      via: "await getTranslations('about')"
      pattern: "getTranslations.*about"
    - from: "src/app/[locale]/about/page.tsx"
      to: "messages/en.json about namespace"
      via: "t('foundingDate'), t('mission'), t('registrationNumber'), etc."
      pattern: "t\\('founding|mission|vision|focusArea|registration"
---

<objective>
Build the About Us page as a single Server Component sourcing all content from next-intl message files.

Purpose: Communicates the organisation's identity, history, mission, and structure to prospective members and the community.
Output: One page file (`src/app/[locale]/about/page.tsx`) covering all four ABOUT requirements using static translated copy — no CMS needed.
</objective>

<execution_context>
@C:/Users/navie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/navie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-static-pages/03-CONTEXT.md
@messages/en.json
@src/app/globals.css
</context>

<interfaces>
<!-- All About Us content comes from next-intl message files — no CMS, no props beyond locale. -->

Message keys available in 'about' namespace after Plan 02 (messages/en.json):
```
title, foundingDate, missionHeading, visionHeading, mission, vision,
focusAreasHeading, focusLeadership, focusCulture, focusCommunity,
focusSocialAwareness, focusWellbeing, registrationHeading,
registrationNumber, registrationLabel
```

next-intl Server Component pattern:
```typescript
import { getTranslations } from 'next-intl/server';
const t = await getTranslations('about');
t('foundingDate')  // → "Founded on 16 September 2002"
```

Brand tokens:
- bg-brand-green / text-brand-green: #32a852
- bg-brand-charcoal: #1a1a1a

Locale layout (src/app/[locale]/layout.tsx) already wraps all pages with Header + Footer.
No need to add navigation or footer to this page file.

Five focus areas to display (all from message keys):
1. focusLeadership — "Leadership"
2. focusCulture — "Cultural Appreciation"
3. focusCommunity — "Community Service"
4. focusSocialAwareness — "Social Awareness"
5. focusWellbeing — "Well-being"
```

Visual treatment is at Claude's discretion per CONTEXT.md.
Recommended: icon-list or card grid using lucide-react icons if appropriate, or plain styled list.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create About Us page with all four ABOUT requirements</name>
  <files>src/app/[locale]/about/page.tsx</files>
  <action>
    Create `src/app/[locale]/about/` directory and `page.tsx`.

    This is a single-file async Server Component — all content comes from next-intl `getTranslations('about')`. No CMS fetches needed; no props beyond what Next.js passes automatically.

    **Page structure:**

    ```tsx
    import { getTranslations } from 'next-intl/server';

    export default async function AboutPage() {
      const t = await getTranslations('about');

      return (
        <main className="max-w-4xl mx-auto px-4 py-16">
          {/* Page heading */}
          <h1 className="text-4xl font-bold mb-2">{t('title')}</h1>
          <p className="text-gray-500 mb-12">{t('foundingDate')}</p>

          {/* Mission & Vision */}
          <section className="mb-12 grid grid-cols-1 md:grid-cols-2 gap-8">
            <div>
              <h2 className="text-2xl font-semibold text-brand-green mb-3">{t('missionHeading')}</h2>
              <p className="text-gray-700 leading-relaxed">{t('mission')}</p>
            </div>
            <div>
              <h2 className="text-2xl font-semibold text-brand-green mb-3">{t('visionHeading')}</h2>
              <p className="text-gray-700 leading-relaxed">{t('vision')}</p>
            </div>
          </section>

          {/* Five Focus Areas */}
          <section className="mb-12">
            <h2 className="text-2xl font-semibold mb-6">{t('focusAreasHeading')}</h2>
            <ul className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
              {[
                t('focusLeadership'),
                t('focusCulture'),
                t('focusCommunity'),
                t('focusSocialAwareness'),
                t('focusWellbeing'),
              ].map((area) => (
                <li
                  key={area}
                  className="flex items-center gap-3 p-4 rounded-lg border border-brand-green/20 bg-brand-green/5"
                >
                  <span className="w-2 h-2 rounded-full bg-brand-green flex-shrink-0" />
                  <span className="font-medium text-gray-800">{area}</span>
                </li>
              ))}
            </ul>
          </section>

          {/* Registration Details */}
          <section className="border-t pt-8">
            <h2 className="text-xl font-semibold mb-4 text-gray-700">{t('registrationHeading')}</h2>
            <p className="text-gray-600">
              <span className="font-medium">{t('registrationLabel')}:</span>{' '}
              {t('registrationNumber')}
            </p>
          </section>
        </main>
      );
    }
    ```

    Do NOT import `Link` or `Button` — this page has no CTAs (none specified in CONTEXT.md).
    Do NOT call any Sanity fetch — all About Us content is static in message files.
    Do NOT use `useTranslations` (hook) — this is a Server Component, use `getTranslations` (async).
    Do NOT hardcode English strings in JSX — all text via `t()`.
  </action>
  <verify>
    <automated>npx vitest run tests/pages/about.test.ts --reporter=verbose</automated>
  </verify>
  <done>All about page tests pass GREEN — page file exists and EN message keys for title, foundingDate, missionHeading, visionHeading, mission, vision, focusAreasHeading, registrationHeading, registrationNumber are all present. `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
Full suite: `npx vitest run --reporter=verbose`

Expected: All about page tests GREEN. All prior tests remain GREEN. Zero TypeScript errors.

Manual spot-check (optional): `npm run dev` → `/en/about` — founding date visible, five focus areas listed, registration number at the bottom. Switch to `/ms/about` and `/ta/about` — all text updates to the correct locale.
</verification>

<success_criteria>
- `src/app/[locale]/about/page.tsx` exists as an async Server Component
- Page renders: page title, founding date (ABOUT-01), mission + vision (ABOUT-02), five focus areas (ABOUT-03), registration number PPM-003-10-20092002 (ABOUT-04)
- All text via `getTranslations('about')` — no hardcoded English
- `npx vitest run tests/pages/about.test.ts` — all tests GREEN
- `npx tsc --noEmit` — exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-static-pages/03-04-SUMMARY.md`
</output>
