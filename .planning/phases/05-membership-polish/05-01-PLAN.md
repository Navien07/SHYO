---
phase: 05-membership-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/pages/membership.test.ts
  - tests/pages/seo.test.ts
  - tests/ui/i18n-messages.test.ts
  - messages/en.json
  - messages/ms.json
  - messages/ta.json
  - .env.local.example
autonomous: true
requirements:
  - MEMB-01
  - MEMB-02
  - MEMB-03
  - MEMB-04
  - MEMB-05
  - MEMB-06
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "All test stubs exist and fail (RED) for membership form, SEO metadata, and privacy page"
    - "All three locale message files have membership, privacy, and meta namespaces with all required keys"
    - "en.json has no duplicate documents key"
    - ".env.local.example documents all required env vars including NEXT_PUBLIC_SITE_URL, ORG_EMAIL, RESEND_FROM"
  artifacts:
    - path: "tests/pages/membership.test.ts"
      provides: "RED test stubs for MEMB-01 through MEMB-05"
      min_lines: 30
    - path: "tests/pages/seo.test.ts"
      provides: "RED test stubs for SEO-01 through SEO-04"
      min_lines: 30
    - path: "messages/en.json"
      provides: "membership, privacy, meta namespaces added; duplicate documents removed"
      contains: "membership"
    - path: "messages/ms.json"
      provides: "membership, privacy, meta namespaces in BM"
      contains: "membership"
    - path: "messages/ta.json"
      provides: "membership, privacy, meta namespaces in Tamil"
      contains: "membership"
  key_links:
    - from: "tests/ui/i18n-messages.test.ts"
      to: "messages/en.json"
      via: "REQUIRED_MEMBERSHIP_KEYS, REQUIRED_PRIVACY_KEYS, REQUIRED_META_KEYS arrays"
      pattern: "REQUIRED_MEMBERSHIP_KEYS|REQUIRED_PRIVACY_KEYS|REQUIRED_META_KEYS"
---

<objective>
Wave 0: Create RED test stubs for all Phase 5 requirements and populate i18n message files with membership, privacy, and meta namespaces in all three locales.

Purpose: Establish the feedback loop — all subsequent plans implement against these test assertions. Also fix the duplicate `documents` key in en.json and document missing env vars.
Output: 3 test files (RED), 3 updated message files, updated .env.local.example
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-membership-polish/05-CONTEXT.md
@.planning/phases/05-membership-polish/05-RESEARCH.md
@.planning/phases/05-membership-polish/05-VALIDATION.md

<interfaces>
<!-- Key patterns the executor needs -->

From tests/ui/i18n-messages.test.ts (existing test pattern):
```typescript
const REQUIRED_NAV_KEYS = ['home', 'about', ...];
// Pattern: define REQUIRED_*_KEYS array, loop with for..of, check messages?.namespace?.[key]
```

From tests/pages/contact.test.ts (existing page test pattern):
```typescript
// Pattern: fs.readFileSync to check file contents, string assertions
```

From messages/en.json (current structure):
```json
{
  "nav": { ... },
  "footer": { ... },
  "NotFound": { ... },
  "home": { ... },
  "about": { ... },
  "documents": { ... },  // DUPLICATE at lines 57-67 and 104-114 — remove first occurrence
  "contact": { ... },
  "team": { ... },
  "programmes": { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test stubs for membership and SEO + extend i18n test</name>
  <files>tests/pages/membership.test.ts, tests/pages/seo.test.ts, tests/ui/i18n-messages.test.ts</files>
  <read_first>
    - tests/ui/i18n-messages.test.ts
    - tests/pages/contact.test.ts
    - tests/pages/homepage.test.ts
  </read_first>
  <action>
    Create `tests/pages/membership.test.ts` following the same `fs.readFileSync` pattern used in existing page tests.

    **Membership test stubs (all must FAIL initially):**

    MEMB-01: Assert these files exist:
    - `src/app/[locale]/membership/page.tsx`
    - `src/app/[locale]/membership/MembershipForm.tsx`
    - `src/app/[locale]/membership/actions.ts`

    MEMB-02: Assert `src/app/[locale]/membership/actions.ts` contains `z.literal(true)` (PDPA consent validation).

    MEMB-03: Assert `src/app/[locale]/membership/actions.ts` contains `'use server'` directive and `z.object` (Zod schema).

    MEMB-04: Assert `src/app/[locale]/membership/actions.ts` contains `parsed.data.email` (sends confirmation to applicant).

    MEMB-05: Assert `src/app/[locale]/membership/actions.ts` contains `ORG_EMAIL` (sends notification to org).

    Create `tests/pages/seo.test.ts`:

    SEO-01: For each page file, assert it contains `generateMetadata`:
    - `src/app/[locale]/page.tsx`
    - `src/app/[locale]/about/page.tsx`
    - `src/app/[locale]/contact/page.tsx`
    - `src/app/[locale]/team/page.tsx`
    - `src/app/[locale]/programmes/page.tsx`
    - `src/app/[locale]/programmes/[slug]/page.tsx`
    - `src/app/[locale]/documents/page.tsx`
    - `src/app/[locale]/membership/page.tsx`
    - `src/app/[locale]/privacy/page.tsx`

    SEO-02: For each page file above, assert it contains `openGraph`.

    SEO-03: Assert these files exist:
    - `src/app/sitemap.ts`
    - `src/app/robots.ts`

    SEO-04: Assert `src/app/[locale]/privacy/page.tsx` exists and contains `PDPA`.

    **Extend `tests/ui/i18n-messages.test.ts`:**

    Add these REQUIRED key arrays and loop assertions for all 3 locales:

    ```typescript
    const REQUIRED_MEMBERSHIP_KEYS = [
      'title', 'fullNameLabel', 'fullNamePlaceholder', 'dobLabel', 'dobPlaceholder',
      'genderLabel', 'genderMale', 'genderFemale', 'genderPreferNot',
      'mobileLabel', 'mobilePlaceholder', 'emailLabel', 'emailPlaceholder',
      'locationLabel', 'locationPlaceholder', 'interestsLabel',
      'interestLeadership', 'interestCultural', 'interestCommunity',
      'interestSocial', 'interestHealth', 'pdpaLabel', 'formSubmit',
      'successTitle', 'successMessage', 'successClose', 'sendError',
      'errorRequired', 'errorEmail', 'errorDobFormat', 'errorInterestsMin', 'errorPdpaRequired'
    ];

    const REQUIRED_PRIVACY_KEYS = ['title', 'lastUpdated', 'section1Heading', 'section1Body', 'section2Heading', 'section2Body', 'section3Heading', 'section3Body', 'section4Heading', 'section4Body', 'section5Heading', 'section5Body'];

    const REQUIRED_META_KEYS = [
      'homeTitle', 'homeDescription', 'aboutTitle', 'aboutDescription',
      'contactTitle', 'contactDescription', 'teamTitle', 'teamDescription',
      'programmesTitle', 'programmesDescription', 'documentsTitle', 'documentsDescription',
      'membershipTitle', 'membershipDescription', 'privacyTitle', 'privacyDescription',
      'adminGuideTitle', 'adminGuideDescription'
    ];
    ```

    Add three new `for` loops (one per key array) inside the existing `for (const locale of ['en', 'ms', 'ta'])` describe block, checking `messages?.membership?.[key]`, `messages?.privacy?.[key]`, and `messages?.meta?.[key]`.
  </action>
  <verify>
    <automated>cd "C:/Users/navie/OneDrive/Documents/ANeura/HYO" && npx vitest run tests/pages/membership.test.ts tests/pages/seo.test.ts --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/pages/membership.test.ts contains `z.literal(true)` as a string assertion
    - tests/pages/membership.test.ts contains `ORG_EMAIL` as a string assertion
    - tests/pages/seo.test.ts contains `generateMetadata` as a string assertion
    - tests/pages/seo.test.ts contains `openGraph` as a string assertion
    - tests/pages/seo.test.ts contains `sitemap.ts` as a path assertion
    - tests/pages/seo.test.ts contains `PDPA` as a string assertion
    - tests/ui/i18n-messages.test.ts contains `REQUIRED_MEMBERSHIP_KEYS`
    - tests/ui/i18n-messages.test.ts contains `REQUIRED_PRIVACY_KEYS`
    - tests/ui/i18n-messages.test.ts contains `REQUIRED_META_KEYS`
    - All membership and SEO tests FAIL (RED) — exit code non-zero
  </acceptance_criteria>
  <done>All test stubs exist. Membership tests fail because no membership files exist yet. SEO tests fail because no generateMetadata/sitemap/robots/privacy files exist yet. i18n tests fail because namespaces not yet added to message files.</done>
</task>

<task type="auto">
  <name>Task 2: Populate i18n namespaces in all three locales + deduplicate en.json + update .env.local.example</name>
  <files>messages/en.json, messages/ms.json, messages/ta.json, .env.local.example</files>
  <read_first>
    - messages/en.json
    - messages/ms.json
    - messages/ta.json
    - .env.local.example
    - .planning/phases/05-membership-polish/05-RESEARCH.md
  </read_first>
  <action>
    **1. Deduplicate en.json:** Remove the FIRST `"documents"` block (lines 57-67) — keep the SECOND one (lines 104-114) which is identical. The JSON parser uses the last occurrence anyway, but having duplicates is a maintenance hazard.

    **2. Add three new namespaces to messages/en.json:**

    Add `"membership"` namespace with these exact keys and values:
    ```json
    "membership": {
      "title": "Be Part of Us",
      "fullNameLabel": "Full Name",
      "fullNamePlaceholder": "e.g. Priya Rajendran",
      "dobLabel": "Date of Birth (DD/MM/YYYY)",
      "dobPlaceholder": "e.g. 15/08/1999",
      "genderLabel": "Gender",
      "genderMale": "Male",
      "genderFemale": "Female",
      "genderPreferNot": "Prefer not to say",
      "mobileLabel": "Mobile Number",
      "mobilePlaceholder": "e.g. 0123456789",
      "emailLabel": "Email Address",
      "emailPlaceholder": "you@example.com",
      "locationLabel": "Area / Location",
      "locationPlaceholder": "e.g. Taman Seputeh, KL",
      "interestsLabel": "Interests (select at least one)",
      "interestLeadership": "Leadership Development",
      "interestCultural": "Cultural Activities",
      "interestCommunity": "Community Service",
      "interestSocial": "Social Awareness",
      "interestHealth": "Health & Well-being",
      "pdpaLabel": "I have read and agree to the Privacy Policy and consent to my data being processed for membership purposes.",
      "formSubmit": "Submit Application",
      "successTitle": "Application Submitted!",
      "successMessage": "Thank you for applying. We'll be in touch soon.",
      "successClose": "Done",
      "sendError": "Sorry, we couldn't submit your application. Please try again.",
      "errorRequired": "This field is required.",
      "errorEmail": "Please enter a valid email address.",
      "errorDobFormat": "Please use DD/MM/YYYY format.",
      "errorInterestsMin": "Please select at least one interest.",
      "errorPdpaRequired": "You must agree to the Privacy Policy before submitting."
    }
    ```

    Add `"privacy"` namespace:
    ```json
    "privacy": {
      "title": "Privacy Policy",
      "lastUpdated": "Last updated: April 2026",
      "section1Heading": "What data we collect",
      "section1Body": "When you submit our membership application form, we collect: your full name, date of birth, gender, mobile number, email address, area/location, and programme interests.",
      "section2Heading": "Why we collect it",
      "section2Body": "We collect this information to process your membership application and to communicate with you about Seputeh HYO programmes and activities.",
      "section3Heading": "How it is stored",
      "section3Body": "Submitted data is received via email only. Seputeh HYO does not operate a database for membership data. Your information is held in the organisation's official email inbox.",
      "section4Heading": "Your rights under PDPA 2010",
      "section4Body": "Under the Personal Data Protection Act 2010 (Malaysia), you have the right to: access your personal data, correct inaccurate data, withdraw consent and cease processing, and inquire about how your data is processed.",
      "section5Heading": "Contact for data requests",
      "section5Body": "To exercise your rights or make a data-related enquiry, please email us at the address listed on our Contact Us page."
    }
    ```

    Add `"meta"` namespace:
    ```json
    "meta": {
      "homeTitle": "Seputeh HYO — Empowering Youth in Seputeh",
      "homeDescription": "Seputeh Hua Youth Organisation — leadership, culture, and community service since 2002.",
      "aboutTitle": "About Us — Seputeh HYO",
      "aboutDescription": "Learn about Seputeh HYO's mission, vision, and five focus areas since 16 September 2002.",
      "contactTitle": "Contact Us — Seputeh HYO",
      "contactDescription": "Get in touch with Seputeh HYO via form, WhatsApp, or email.",
      "teamTitle": "Our Team — Seputeh HYO",
      "teamDescription": "Meet the committee members leading Seputeh HYO.",
      "programmesTitle": "Our Programmes — Seputeh HYO",
      "programmesDescription": "Explore Seputeh HYO's programmes across leadership, culture, community, and well-being.",
      "documentsTitle": "Documents — Seputeh HYO",
      "documentsDescription": "Download official documents from Seputeh HYO.",
      "membershipTitle": "Join Us — Seputeh HYO",
      "membershipDescription": "Apply to become a member of Seputeh HYO and be part of the community.",
      "privacyTitle": "Privacy Policy — Seputeh HYO",
      "privacyDescription": "How Seputeh HYO collects, uses, and protects your personal data under PDPA 2010.",
      "adminGuideTitle": "Admin Guide — Seputeh HYO",
      "adminGuideDescription": "Quick reference guide for Seputeh HYO content administrators."
    }
    ```

    **3. Add same three namespaces to messages/ms.json** with BM translations (approximate — same as prior phases, native-speaker review before launch):
    - `membership.title`: "Sertai Kami"
    - `membership.successClose`: "Selesai"
    - `privacy.title`: "Dasar Privasi"
    - All other keys translated to BM equivalents
    - `meta` namespace: BM versions of all page titles and descriptions

    **4. Add same three namespaces to messages/ta.json** with Tamil translations (approximate):
    - `membership.title`: "எங்களுடன் இணையுங்கள்"
    - `membership.successClose`: "முடிந்தது"
    - `privacy.title`: "தனியுரிமைக் கொள்கை"
    - All other keys translated to Tamil equivalents
    - `meta` namespace: Tamil versions of all page titles and descriptions

    **5. Update .env.local.example** — append these lines:
    ```
    ORG_EMAIL=                         # Organisation email for receiving form submissions
    RESEND_FROM=                       # Resend verified sender (e.g. noreply@seputehhyo.org) — falls back to onboarding@resend.dev
    NEXT_PUBLIC_SITE_URL=              # Canonical base URL (e.g. https://seputehhyo.org) — used in sitemap.xml and OG tags
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/navie/OneDrive/Documents/ANeura/HYO" && npx vitest run tests/ui/i18n-messages.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json contains `"membership"` namespace with key `"successClose": "Done"`
    - messages/en.json contains `"privacy"` namespace with key `"section4Heading": "Your rights under PDPA 2010"`
    - messages/en.json contains `"meta"` namespace with key `"homeTitle"`
    - messages/en.json does NOT have two `"documents"` keys (grep returns exactly 1 match for `"documents":`)
    - messages/ms.json contains `"membership"` namespace with key `"title": "Sertai Kami"`
    - messages/ta.json contains `"membership"` namespace with key `"title"`
    - .env.local.example contains `NEXT_PUBLIC_SITE_URL=`
    - .env.local.example contains `ORG_EMAIL=`
    - .env.local.example contains `RESEND_FROM=`
    - i18n-messages.test.ts passes for membership, privacy, and meta namespace assertions in all 3 locales
  </acceptance_criteria>
  <done>All three message files have membership, privacy, and meta namespaces. en.json duplicate documents key removed. .env.local.example documents all env vars. i18n tests pass for new namespaces. Membership and SEO file-existence tests still fail (RED) — correct, as implementation comes in Plans 02-04.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/pages/membership.test.ts` — all stubs FAIL (RED)
- `npx vitest run tests/pages/seo.test.ts` — all stubs FAIL (RED)
- `npx vitest run tests/ui/i18n-messages.test.ts` — all namespace assertions PASS (GREEN)
- `node -e "const j=JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log(Object.keys(j))"` — shows membership, privacy, meta without duplicate documents
</verification>

<success_criteria>
- 3 test files created/updated with RED stubs covering MEMB-01-05, SEO-01-04, MEMB-06
- 3 message files updated with 3 new namespaces each
- en.json deduplicated
- .env.local.example updated with 3 new env vars
- Existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-membership-polish/05-01-SUMMARY.md`
</output>
