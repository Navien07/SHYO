---
phase: 05-membership-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/app/[locale]/page.tsx
  - src/app/[locale]/about/page.tsx
  - src/app/[locale]/contact/page.tsx
  - src/app/[locale]/team/page.tsx
  - src/app/[locale]/programmes/page.tsx
  - src/app/[locale]/programmes/[slug]/page.tsx
  - src/app/[locale]/documents/page.tsx
  - src/app/[locale]/membership/page.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03

must_haves:
  truths:
    - "Every page has a unique <title> and <meta description> in the active locale"
    - "Every page has og:title, og:description, og:url, og:image, og:type in page source"
    - "sitemap.xml lists all locale-prefixed URLs including dynamic programme slugs"
    - "robots.txt allows all crawlers and points to sitemap.xml"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap with all locale-prefixed URLs and programme slugs"
      exports: ["default"]
      contains: "MetadataRoute.Sitemap"
    - path: "src/app/robots.ts"
      provides: "robots.txt allowing all crawlers"
      exports: ["default"]
      contains: "MetadataRoute.Robots"
    - path: "src/app/[locale]/page.tsx"
      provides: "generateMetadata with openGraph for homepage"
      contains: "generateMetadata"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/sanity/queries.ts"
      via: "getAllProgrammes() for dynamic slugs"
      pattern: "getAllProgrammes"
    - from: "src/app/[locale]/page.tsx"
      to: "messages/en.json"
      via: "getTranslations({ locale, namespace: 'meta' })"
      pattern: "getTranslations.*meta"
---

<objective>
Add SEO metadata (generateMetadata + Open Graph) to all pages, create sitemap.ts and robots.ts.

Purpose: Ensure every page is indexed correctly with unique locale-aware titles, descriptions, and social sharing tags. This satisfies SEO-01, SEO-02, and SEO-03.
Output: generateMetadata added to 8 existing page files (privacy page handled by Plan 04), sitemap.ts and robots.ts created
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-membership-polish/05-CONTEXT.md
@.planning/phases/05-membership-polish/05-RESEARCH.md
@.planning/phases/05-membership-polish/05-01-SUMMARY.md

<interfaces>
<!-- Pattern for generateMetadata — apply to all pages -->

From 05-RESEARCH.md Pattern 1:
```typescript
import { getTranslations } from 'next-intl/server';
import type { Metadata } from 'next';

type Props = { params: Promise<{ locale: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'meta' });
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://seputehhyo.org';

  return {
    title: t('homeTitle'),
    description: t('homeDescription'),
    openGraph: {
      title: t('homeTitle'),
      description: t('homeDescription'),
      url: `${baseUrl}/${locale}`,
      siteName: 'Seputeh HYO',
      images: [{ url: `${baseUrl}/logo.svg`, width: 512, height: 512, alt: 'Seputeh HYO' }],
      type: 'website',
      locale,
    },
  };
}
```

From src/lib/sanity/queries.ts:
```typescript
export async function getAllProgrammes(): Promise<Programme[]>
// Returns programmes with slug.current field
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generateMetadata with Open Graph to all existing page files</name>
  <files>src/app/[locale]/page.tsx, src/app/[locale]/about/page.tsx, src/app/[locale]/contact/page.tsx, src/app/[locale]/team/page.tsx, src/app/[locale]/programmes/page.tsx, src/app/[locale]/programmes/[slug]/page.tsx, src/app/[locale]/documents/page.tsx, src/app/[locale]/membership/page.tsx</files>
  <read_first>
    - src/app/[locale]/page.tsx
    - src/app/[locale]/about/page.tsx
    - src/app/[locale]/contact/page.tsx
    - src/app/[locale]/team/page.tsx
    - src/app/[locale]/programmes/page.tsx
    - src/app/[locale]/programmes/[slug]/page.tsx
    - src/app/[locale]/documents/page.tsx
    - src/app/[locale]/membership/page.tsx
  </read_first>
  <action>
    Add `generateMetadata` export to each page file. Each page already has `params: Promise<{ locale: string }>` — reuse the Props type or define locally.

    **Every page needs these imports added (if not present):**
    ```typescript
    import { getTranslations } from 'next-intl/server';
    import type { Metadata } from 'next';
    ```

    **Common helper pattern** (define in each file or inline):
    ```typescript
    const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://seputehhyo.org';
    ```

    **Per-page generateMetadata — each uses different meta keys and URL paths:**

    1. **Home** (`src/app/[locale]/page.tsx`):
       - title: `t('homeTitle')`, description: `t('homeDescription')`
       - url: `${baseUrl}/${locale}`
       - priority: 1.0 (homepage)

    2. **About** (`src/app/[locale]/about/page.tsx`):
       - title: `t('aboutTitle')`, description: `t('aboutDescription')`
       - url: `${baseUrl}/${locale}/about`

    3. **Contact** (`src/app/[locale]/contact/page.tsx`):
       - title: `t('contactTitle')`, description: `t('contactDescription')`
       - url: `${baseUrl}/${locale}/contact`

    4. **Team** (`src/app/[locale]/team/page.tsx`):
       - title: `t('teamTitle')`, description: `t('teamDescription')`
       - url: `${baseUrl}/${locale}/team`

    5. **Programmes listing** (`src/app/[locale]/programmes/page.tsx`):
       - title: `t('programmesTitle')`, description: `t('programmesDescription')`
       - url: `${baseUrl}/${locale}/programmes`

    6. **Programme detail** (`src/app/[locale]/programmes/[slug]/page.tsx`):
       - title: `t('programmesTitle')`, description: `t('programmesDescription')`
       - url: `${baseUrl}/${locale}/programmes/${slug}` — slug from `await params`
       - Note: params includes both `locale` and `slug`

    7. **Documents** (`src/app/[locale]/documents/page.tsx`):
       - title: `t('documentsTitle')`, description: `t('documentsDescription')`
       - url: `${baseUrl}/${locale}/documents`

    8. **Membership** (`src/app/[locale]/membership/page.tsx`):
       - title: `t('membershipTitle')`, description: `t('membershipDescription')`
       - url: `${baseUrl}/${locale}/membership`

    **Open Graph object (same structure on every page):**
    ```typescript
    openGraph: {
      title: t('...Title'),
      description: t('...Description'),
      url: `${baseUrl}/${locale}/path`,
      siteName: 'Seputeh HYO',
      images: [{ url: `${baseUrl}/logo.svg`, width: 512, height: 512, alt: 'Seputeh HYO' }],
      type: 'website',
      locale,
    },
    ```

    **Critical:** Use `getTranslations({ locale, namespace: 'meta' })` — NOT `useTranslations` which is client-only. Always `await params` before accessing `locale`. The `getTranslations` import is from `'next-intl/server'`.

    **Do NOT change any existing page component logic** — only add the `generateMetadata` export and its required imports.
  </action>
  <verify>
    <automated>cd "C:/Users/navie/OneDrive/Documents/ANeura/HYO" && npx vitest run tests/pages/seo.test.ts --reporter=verbose 2>&1 | grep -E "generateMetadata|openGraph|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[locale]/page.tsx contains `export async function generateMetadata`
    - src/app/[locale]/page.tsx contains `openGraph`
    - src/app/[locale]/about/page.tsx contains `generateMetadata` and `openGraph`
    - src/app/[locale]/contact/page.tsx contains `generateMetadata` and `openGraph`
    - src/app/[locale]/team/page.tsx contains `generateMetadata` and `openGraph`
    - src/app/[locale]/programmes/page.tsx contains `generateMetadata` and `openGraph`
    - src/app/[locale]/programmes/[slug]/page.tsx contains `generateMetadata` and `openGraph`
    - src/app/[locale]/documents/page.tsx contains `generateMetadata` and `openGraph`
    - src/app/[locale]/membership/page.tsx contains `generateMetadata` and `openGraph`
    - All pages import `getTranslations` from `next-intl/server` (NOT `useTranslations`)
    - All pages use `namespace: 'meta'` in getTranslations call
    - SEO-01 and SEO-02 tests pass for these 8 pages
  </acceptance_criteria>
  <done>All 8 existing page files have generateMetadata with Open Graph tags. Each page gets unique locale-aware title and description from the meta namespace. SEO-01 and SEO-02 tests GREEN for these pages.</done>
</task>

<task type="auto">
  <name>Task 2: Create sitemap.ts and robots.ts</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <read_first>
    - src/lib/sanity/queries.ts
    - .planning/phases/05-membership-polish/05-RESEARCH.md
  </read_first>
  <action>
    **Create `src/app/sitemap.ts`:**
    ```typescript
    import type { MetadataRoute } from 'next';
    import { getAllProgrammes } from '@/lib/sanity/queries';

    const BASE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://seputehhyo.org';
    const LOCALES = ['en', 'ms', 'ta'] as const;
    const STATIC_PATHS = [
      '',           // home
      '/about',
      '/contact',
      '/team',
      '/programmes',
      '/documents',
      '/membership',
      '/privacy',
    ];

    export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
      let programmes: { slug?: { current?: string } }[] = [];
      try {
        programmes = await getAllProgrammes();
      } catch {
        // Fallback: if Sanity unavailable at build time, generate sitemap without dynamic entries
        programmes = [];
      }

      const staticEntries = STATIC_PATHS.flatMap((path) =>
        LOCALES.map((locale) => ({
          url: `${BASE_URL}/${locale}${path}`,
          lastModified: new Date(),
          changeFrequency: (path === '' ? 'weekly' : 'monthly') as 'weekly' | 'monthly',
          priority: path === '' ? 1.0 : 0.8,
        }))
      );

      const programmeEntries = LOCALES.flatMap((locale) =>
        programmes
          .filter((p) => p.slug?.current)
          .map((p) => ({
            url: `${BASE_URL}/${locale}/programmes/${p.slug!.current}`,
            lastModified: new Date(),
            changeFrequency: 'monthly' as const,
            priority: 0.6,
          }))
      );

      return [...staticEntries, ...programmeEntries];
    }
    ```

    **Important:** Do NOT include `/admin-guide` in STATIC_PATHS — it is excluded from sitemap per CONTEXT.md decision.

    **Create `src/app/robots.ts`:**
    ```typescript
    import type { MetadataRoute } from 'next';

    const BASE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://seputehhyo.org';

    export default function robots(): MetadataRoute.Robots {
      return {
        rules: { userAgent: '*', allow: '/' },
        sitemap: `${BASE_URL}/sitemap.xml`,
      };
    }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/navie/OneDrive/Documents/ANeura/HYO" && npx vitest run tests/pages/seo.test.ts --reporter=verbose 2>&1 | grep -E "sitemap|robots|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/sitemap.ts exists and contains `MetadataRoute.Sitemap`
    - src/app/sitemap.ts contains `getAllProgrammes` import
    - src/app/sitemap.ts contains `try` and `catch` around getAllProgrammes call
    - src/app/sitemap.ts contains all 8 static paths: '', '/about', '/contact', '/team', '/programmes', '/documents', '/membership', '/privacy'
    - src/app/sitemap.ts does NOT contain 'admin-guide'
    - src/app/robots.ts exists and contains `MetadataRoute.Robots`
    - src/app/robots.ts contains `sitemap:` directive string
    - SEO-03 test assertions PASS
  </acceptance_criteria>
  <done>sitemap.ts generates all locale-prefixed URLs (3 locales x 8 static paths = 24 entries + dynamic programme entries). robots.ts allows all crawlers and points to sitemap.xml. SEO-03 tests GREEN.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/pages/seo.test.ts` — all SEO-01, SEO-02, SEO-03 tests PASS
- `npx vitest run` — full suite passes
- `npx next build` — compiles without errors (sitemap.ts and robots.ts generate correctly)
</verification>

<success_criteria>
- Every page has generateMetadata with locale-aware title, description, and Open Graph tags
- sitemap.xml covers all locale-prefixed URLs including programme detail pages
- robots.txt allows crawling and references sitemap
- No existing tests regressed
</success_criteria>

<output>
After completion, create `.planning/phases/05-membership-polish/05-03-SUMMARY.md`
</output>
