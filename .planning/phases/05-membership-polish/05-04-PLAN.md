---
phase: 05-membership-polish
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/[locale]/privacy/page.tsx
  - src/app/[locale]/admin-guide/page.tsx
autonomous: false
requirements:
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "Privacy Policy page is accessible at /en/privacy, /ms/privacy, /ta/privacy with PDPA 2010 content"
    - "Privacy page has all 5 required sections: what data, why collected, how stored, rights under PDPA, contact"
    - "Admin Guide page is accessible at /en/admin-guide with 6 sections covering Sanity Studio usage"
    - "Admin Guide page has noindex robots meta tag via generateMetadata"
    - "HTTPS is enforced on the deployed Vercel URL"
    - "Responsive QA passes: no horizontal scroll, touch targets >= 44px on membership form, colour contrast on form elements, Tamil rendering on membership form"
  artifacts:
    - path: "src/app/[locale]/privacy/page.tsx"
      provides: "Static Privacy Policy page with PDPA 2010 sections in all 3 locales"
      contains: "PDPA"
    - path: "src/app/[locale]/admin-guide/page.tsx"
      provides: "Static Admin Guide page (EN only) with 6 Sanity Studio sections"
      contains: "admin-guide"
  key_links:
    - from: "src/app/[locale]/privacy/page.tsx"
      to: "messages/en.json"
      via: "getTranslations with namespace privacy"
      pattern: "getTranslations.*privacy"
    - from: "src/components/layout/Footer.tsx"
      to: "src/app/[locale]/privacy/page.tsx"
      via: "Link href /privacy from i18n navigation"
      pattern: "href.*privacy"
---

<objective>
Create the Privacy Policy page (all 3 locales) and Admin Guide page (EN only). Verify HTTPS enforcement on Vercel and run responsive/accessibility QA checklist.

Purpose: Privacy Policy satisfies PDPA 2010 legal requirement (SEO-04). Admin Guide enables non-technical org admins to manage the site independently. HTTPS verification confirms SEO-05. QA checklist confirms responsive and accessibility compliance on new Phase 5 elements.
Output: 2 new page files, HTTPS confirmation, QA sign-off
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-membership-polish/05-CONTEXT.md
@.planning/phases/05-membership-polish/05-RESEARCH.md
@.planning/phases/05-membership-polish/05-UI-SPEC.md
@.planning/phases/05-membership-polish/05-01-SUMMARY.md

<interfaces>
From src/app/[locale]/about/page.tsx (page pattern):
- import getTranslations from next-intl/server
- interface with params: Promise of locale string
- export default async function, await params, getTranslations with locale and namespace
- main with max-w-6xl mx-auto px-4 py-16

From messages/en.json (privacy namespace added in Plan 01):
- privacy.title, privacy.lastUpdated
- privacy.section1Heading through section5Heading
- privacy.section1Body through section5Body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Privacy Policy page with PDPA 2010 sections and generateMetadata</name>
  <files>src/app/[locale]/privacy/page.tsx</files>
  <read_first>
    - src/app/[locale]/about/page.tsx
    - messages/en.json
    - src/components/layout/Footer.tsx
    - .planning/phases/05-membership-polish/05-UI-SPEC.md
    - .planning/phases/05-membership-polish/05-RESEARCH.md
  </read_first>
  <action>
    Create src/app/[locale]/privacy/page.tsx with two exports: generateMetadata and default PrivacyPage.

    IMPORTS: getTranslations from next-intl/server, Metadata type from next.

    GENERATE METADATA EXPORT:
    - Define Props type with params: Promise of locale string
    - Await params to get locale
    - Call getTranslations with locale and namespace meta
    - Define baseUrl as process.env.NEXT_PUBLIC_SITE_URL falling back to https://seputehhyo.org
    - Return object with title from t privacyTitle, description from t privacyDescription
    - Include openGraph with title, description, url as baseUrl/locale/privacy, siteName Seputeh HYO, images array with baseUrl/logo.svg at 512x512, type website, locale

    PAGE COMPONENT:
    - Interface PrivacyPageProps with params Promise of locale string
    - Export default async function PrivacyPage accepting params
    - Await params to get locale
    - Call getTranslations with locale and namespace privacy (for page content)
    - Build sections array with 5 objects, each having heading from t section1Heading through section5Heading and body from t section1Body through section5Body
    - Return main element with className max-w-3xl mx-auto px-4 py-16
    - H1 with className text-4xl font-bold mb-8, text from t title
    - P with className text-sm text-muted-foreground mb-12, text from t lastUpdated
    - Map over sections array, each rendered as section with className mb-8
    - H2 with className text-xl font-normal mb-3 (NOT font-bold, per UI-SPEC)
    - P with className text-base leading-relaxed for body text

    LAYOUT: max-w-3xl, plain prose, no card backgrounds. Font weights: H1 bold, H2 font-normal.

    FOOTER LINK: Already exists in Footer.tsx using Link from @/i18n/navigation pointing to /privacy. No Footer changes needed.
  </action>
  <verify>
    <automated>cd "C:/Users/navie/OneDrive/Documents/ANeura/HYO" && npx vitest run tests/pages/seo.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[locale]/privacy/page.tsx exists
    - File contains generateMetadata export
    - File contains openGraph in the metadata return
    - File contains getTranslations from next-intl/server
    - File contains namespace privacy for page content
    - File contains namespace meta for metadata
    - File uses max-w-3xl layout
    - File uses font-normal on H2 headings
    - SEO-04 test assertions PASS
  </acceptance_criteria>
  <done>Privacy Policy page renders 5 PDPA 2010 sections in all 3 locales with proper SEO metadata. Footer link already works. SEO-04 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Create Admin Guide page with noindex and 6 Sanity sections</name>
  <files>src/app/[locale]/admin-guide/page.tsx</files>
  <read_first>
    - src/app/[locale]/about/page.tsx
    - .planning/phases/05-membership-polish/05-CONTEXT.md
    - .planning/phases/05-membership-polish/05-UI-SPEC.md
  </read_first>
  <action>
    Create src/app/[locale]/admin-guide/page.tsx with EN-only hardcoded content.

    IMPORTS: getTranslations from next-intl/server, Metadata type from next.

    GENERATE METADATA WITH NOINDEX:
    - Define Props type with params Promise of locale string
    - Await params to get locale
    - Call getTranslations with locale and namespace meta
    - Return object with title from t adminGuideTitle, description from t adminGuideDescription
    - Add robots object with index false, follow false
    - No openGraph needed since page is noindex

    PAGE COMPONENT:
    - Export default async function AdminGuidePage (no props needed for content since EN-only)
    - Return main with className max-w-3xl mx-auto px-4 py-16
    - H1 text-4xl font-bold mb-2 with text Admin Guide
    - P text-base text-muted-foreground mb-12 with subtitle Quick reference for Seputeh HYO content administrators.
    - 6 sections, each with className mb-8:

    Section 1 H2: 1. How to log into Sanity Studio
    Body: Navigate to /studio on the website (e.g. seputehhyo.org/studio). Log in with your organisation email address. If you do not have access, ask the site administrator to invite you.

    Section 2 H2: 2. Adding or editing a team member
    Body: In Sanity Studio, click Team Member in the left sidebar. Click + Create to add a new member. Fill in the name (in English, Bahasa Malaysia, and Tamil), select a role (President, VP, Secretary, etc.), upload a photo, and set the display order. Click Publish to make the changes live.

    Section 3 H2: 3. Adding or editing a programme
    Body: Click Programme in the sidebar. Click + Create. Fill in the title, description, and category in all three languages. Upload a cover photo and set the slug (URL-friendly name). Click Publish.

    Section 4 H2: 4. Uploading a document (PDF)
    Body: Click PDF Document in the sidebar. Click + Create. Fill in the display title in all three languages, select a category, enter the year, and upload the PDF file. All fields are required. Click Publish.

    Section 5 H2: 5. Updating site settings
    Body: Click Site Settings in the sidebar. This is a singleton document. Update the contact email, WhatsApp number, Facebook URL, or Instagram URL as needed. Click Publish to apply settings site-wide.

    Section 6 H2: 6. How content updates appear live (ISR)
    Body: When you click Publish in Sanity Studio, the website automatically refreshes the affected pages within a few seconds. This is called Incremental Static Regeneration (ISR). You do not need to redeploy the website or contact a developer.

    All H2 elements use className text-xl font-normal mb-3.
    All body P elements use className text-base leading-relaxed.
    Use code element with className bg-gray-100 px-1 rounded for /studio path.
    Use strong elements for Sanity UI labels (Team Member, Programme, etc.).

    NOT linked from nav or footer. Direct URL access only per CONTEXT.md.
  </action>
  <verify>
    <automated>cd "C:/Users/navie/OneDrive/Documents/ANeura/HYO" && npx vitest run tests/pages/seo.test.ts --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[locale]/admin-guide/page.tsx exists
    - File contains generateMetadata export
    - File contains index: false in robots metadata
    - File contains 6 H2 section headings (numbered 1-6)
    - File contains text ISR in section 6
    - File contains text Sanity Studio
    - File does NOT contain openGraph (noindex page)
    - File uses max-w-3xl layout
    - File uses font-normal on H2 headings
  </acceptance_criteria>
  <done>Admin Guide page has 6 instructional sections for Sanity Studio admin tasks, noindex metadata, and is not linked from navigation. Accessible at /locale/admin-guide via direct URL only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify HTTPS enforcement on Vercel (SEO-05)</name>
  <what-built>Vercel automatically provisions SSL certificates for all deployments. This task confirms HTTPS is active on the deployed URL — no code change required, this is infrastructure verification.</what-built>
  <how-to-verify>
    1. Open the deployed Vercel URL in your browser (e.g. https://seputehhyo.org or the preview URL)
    2. Confirm the browser shows a padlock icon (HTTPS active)
    3. Try navigating to http://seputehhyo.org — confirm it redirects to https://
    4. Check that no mixed-content warnings appear in browser console
  </how-to-verify>
  <resume-signal>Type "approved" if HTTPS is active, or describe any SSL issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Responsive QA and accessibility checklist</name>
  <what-built>Phase 5 added the membership form (Plan 02), privacy page (Task 1 above), and admin guide (Task 2 above). This checklist verifies responsive behaviour and WCAG 2.1 AA compliance on these new elements.</what-built>
  <how-to-verify>
    **Responsive QA (test in all 3 locales: /en/, /ms/, /ta/):**
    1. Open /[locale]/membership on mobile viewport (375px) — confirm NO horizontal scroll
    2. Open /[locale]/membership on tablet viewport (768px) — confirm NO horizontal scroll
    3. Open /[locale]/membership on desktop viewport (1280px) — confirm layout uses available width appropriately
    4. Repeat steps 1-3 for /[locale]/privacy

    **Touch targets (mobile viewport):**
    5. On /[locale]/membership — confirm all form inputs, checkboxes (interests + PDPA), and submit button have touch targets >= 44px height
    6. Use browser DevTools to measure checkbox label height (should be min-h-[44px])

    **WCAG 2.1 AA colour contrast:**
    7. On /[locale]/membership — run axe DevTools or Lighthouse accessibility audit
    8. Confirm colour contrast >= 4.5:1 on all form labels, error messages, and placeholder text
    9. Confirm the brand-green submit button text has sufficient contrast against bg-brand-green

    **Tamil rendering:**
    10. Open /ta/membership on a mid-range Android device (or Chrome DevTools mobile emulation)
    11. Confirm Noto Sans Tamil renders all form labels, error messages, and placeholder text without clipped descenders or missing glyphs
    12. Confirm Tamil text in interests checkboxes renders correctly

    **Keyboard navigation:**
    13. On /en/membership — tab through all form fields, confirm logical tab order and visible focus rings
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list specific failures (e.g. "horizontal scroll on /ta/membership at 375px", "PDPA checkbox touch target only 32px")</resume-signal>
</task>

</tasks>

<verification>
- npx vitest run tests/pages/seo.test.ts -- SEO-04 (privacy page) tests PASS
- npx vitest run -- full suite passes (no regressions)
- Privacy page has generateMetadata with OG tags
- Admin guide has generateMetadata with noindex
- HTTPS confirmed active on deployed URL (manual)
- Responsive QA checklist passed (manual)
</verification>

<success_criteria>
- Privacy Policy accessible at /en/privacy with 5 PDPA 2010 sections, translated in all 3 locales
- Admin Guide accessible at /en/admin-guide with 6 Sanity sections, EN only, noindex
- Both pages have proper generateMetadata exports
- SEO-04 tests GREEN
- HTTPS enforced on Vercel (SEO-05 confirmed)
- Responsive QA passed: no horizontal scroll, touch targets >= 44px, colour contrast compliant, Tamil rendering correct
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create .planning/phases/05-membership-polish/05-04-SUMMARY.md
</output>
